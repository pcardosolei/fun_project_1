#Local Imports
import csvReader

def main():
  """ Main function that will work for the whole application
        -- phase 1 working with the CSV
        -- phase 2 read the content on the csv and prepare it
        -- step 3 analysis the data . try error in some situations. use R if needed
        -- data science to get results.
        -- step 5 -- iterate with more data ( return to step 3 with more data)
  """


if __name__ == "__main__":
  main()
